<div>
<p><%= l :private_comments_for_groups%></p>
<% if User.current.allowed_to?(:manage_group_access_to_notes, @project)%>
	<div id="edit-readers-groups">
			<p><% Group.all.each do |group| %>
				<%= label_tag 'group_ids[]', group.name %>
				<%= check_box_tag 'group_ids[]', group.id, aDefGroups.include?(group) -%>
			<% end%></p>		
	</div>
<% end%>
</div>